import { styled } from '..'

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  height: '100vh',
  minHeight: '100vh',
  position: 'relative',
  overflow: 'hidden',
})
